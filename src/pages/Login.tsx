import { Box, Flex } from "@chakra-ui/react";
import { Form } from "../components/Form";

export const Login = () => (
  <>
    <Flex flexDirection={"row"} height={"100vh"} overflow={"hidden"}>
      <Form />
    </Flex>
  </>
);
